[build-system]
requires = ["setuptools >= 61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "lakebase_responders_entities"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = [
  "sqlmodel==0.0.25"
]

[tool.setuptools.packages.find]
where = ["src"]